{
  "name": "@si2/mobile",
  "private": true,
  "scripts": {
    "dev": "flutter run -d web-server --web-hostname=localhost --web-port=5177",
    "android": "flutter run -d android",
    "windows": "flutter run -d windows",
    "build:web": "flutter build web",
    "build:apk": "flutter build apk --release"
  }
}
