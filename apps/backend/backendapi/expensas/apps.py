from django.apps import AppConfig

class ExpensasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "backendapi.expensas"
    label = "expensas"
    verbose_name = "Módulo de Expensas"
